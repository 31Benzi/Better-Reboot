# Backend assets (generated)

Static backend data is **not** stored in git here. The canonical copy is in [`auth_backend/`](../../../auth_backend/).

Before building or running the GUI, sync assets:

```bat
scripts\sync-backend-assets.bat
```

`build.bat` and `start.bat` run this automatically.
